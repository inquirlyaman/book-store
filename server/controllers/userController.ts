import {UserService} from '../services/userService';
import { RequestHandler } from "express";
import * as _ from 'lodash';

export class UserController {
    updateRoles: RequestHandler = (req?: any, res?: any, next?: any) => {
        try {
            // TODO: Add the logic

            
        } catch (error) {
            console.log(error);
        }
    }
}